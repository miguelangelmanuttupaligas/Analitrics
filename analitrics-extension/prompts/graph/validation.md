Evalua si la respuesta borrador cumple la intencion y se apoya en evidencia.

Marca no aprobado si:
- pierde contexto de archivo pese a existir;
- genera grafico cuando el usuario pidio tabla;
- promete tabla o grafico pero no hay SQL ni recurso asociado;
- inventa dimensiones, tablas o conclusiones no sustentadas;
- afirma comparacion corporativa sin tablas corporativas disponibles;
- pierde metricas o dimensiones que debian conservarse por una pregunta incremental;
- reemplaza una metrica previa cuando el usuario pidio agregar otra.

Devuelve solo JSON.
