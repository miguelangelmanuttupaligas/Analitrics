HOST=localhost
PORT=3080
DOMAIN_CLIENT=http://localhost:3080
DOMAIN_SERVER=http://localhost:3080
MONGO_URI=mongodb://mongodb:27017/LibreChat

ALLOW_EMAIL_LOGIN=true
ALLOW_REGISTRATION=true
ALLOW_SOCIAL_LOGIN=false
ALLOW_SOCIAL_REGISTRATION=false

NO_INDEX=true
TRUST_PROXY=1

JWT_SECRET=replace_with_64_hex_chars
JWT_REFRESH_SECRET=replace_with_64_hex_chars
CREDS_KEY=replace_with_64_hex_chars
CREDS_IV=replace_with_32_hex_chars

OPENAI_API_KEY=user_provided
ASSISTANTS_API_KEY=user_provided

# Required for file embeddings in LibreChat RAG.
# Replace `user_provided` with a real key before testing CSV/XLSX uploads.
RAG_OPENAI_API_KEY=user_provided
EMBEDDINGS_PROVIDER=openai
EMBEDDINGS_MODEL=text-embedding-3-small
RAG_PORT=8000
MCP_TIMEOUT=20000

# PostgreSQL del RAG/vector store interno
VECTOR_DB_NAME=analitrics_rag
VECTOR_DB_USER=analitrics_rag
VECTOR_DB_PASSWORD=replace_with_random_string

# PostgreSQL propia de Analitrics para:
# - metadata de archivos
# - tablas importadas desde CSV/XLSX
# - datos corporativos de la demo
ANALITRICS_POSTGRES_DB=analitrics
ANALITRICS_POSTGRES_USER=analitrics
ANALITRICS_POSTGRES_PASSWORD=replace_with_random_string
ANALITRICS_POSTGRES_PORT=5436

# Modelo por defecto usado por la extensión de Analitrics
WORKER_MODEL=gpt-4.1-mini

# Costo estimado por 1M tokens para reportes de evaluación.
# Ajustar según el precio vigente del modelo configurado.
LLM_INPUT_COST_PER_1M=0.4
LLM_OUTPUT_COST_PER_1M=1.6

# Presupuestos de contexto para el snapshot que consumen los workers
CONTEXT_MAX_ASSETS=4
CONTEXT_MAX_TABLES_PER_ASSET=3
CONTEXT_MAX_COLUMNS_PER_TABLE=12
CONTEXT_MAX_SAMPLE_ROWS_PER_TABLE=3
